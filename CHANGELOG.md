# Changelog

## Unreleased

- pastaの時間依存テストを待ち合わせ方式へ変更し、CIでの偶発的な失敗を防ぎました。
- ese-shioriの辞書処理を通常のSHIORI入口を持つdylibへ移し、状態と書き込みファイルをゴーストの外へ保存できるようにしました。
- カタログで原版の1〜3区切りの数字バージョンをそのまま表示できるようにしました。
- 偽栞の辞書・状態処理を独立したSwift製dylibに移し、通常のSHIORI入口と保存先の指定に対応しました。
- カタログの原作リンクでHTTPの旧サイトも開けるようにしました。
- カタログに原作のリポジトリ・保存版サイトへのリンクを追加しました。
- カタログ上で導入手順とライセンス本文をポップアップ表示できるようにしました。
- Swift製モジュールのビルド領域を共用し、共通部分の再コンパイルを減らしました。
- カタログの一覧・検索・種類別フィルタ・ダウンロードを行う簡素な静的ページを生成できるようにしました。
- 配布ZIPのファイル名に内容ハッシュを付け、一覧の更新中も既存のZIPを残せるようにしました。
- 別のmacOS環境で同じZIPを実行した結果をSHA-256で照合し、カタログへ反映できるようにしました。
- 署名済みカタログをFTPSで配置するCIを追加しました。検証用と正式配布用の配置先を分けます。
- CIで1組のUniversal ZIPをarm64でビルドし、同じZIPをIntelでも実行確認するようにしました。
- カタログ一式を検証し、Ed25519で署名した検証用スナップショットを作成できるようにしました。正式配布への昇格には全バイナリの実行確認を要求します。

- YAYA・里々のUniversal dylibと、同梱・共通導入したSAORI dylibを読み込む接続を追加しました。

- 内蔵SAORI 5種類を独立したSwift実装として切り出し、個別のUniversal dylibをビルド・検証できるようにしました。

- pastaのmacOS移植版を追加し、通常のSHIORI入口を持つUniversal ZIPをビルド・検証できるようにしました。

- 美坂のライブラリ名を`libmisaka.dylib`に統一し、ゴースト直下へ同梱する手順を整えました。

- minatoのmacOS移植版を追加し、固定ソースからUniversal ZIPをビルド・検証できるようにしました。

- kagariの固定ソースから、依存ライブラリ・ライセンス・ビルド情報を含むmacOS向けZIPを作成できるようにしました。
- Utataneの独自美坂実装をSwift Packageとして切り出し、動的ライブラリとして配布できるようにしました。
- 美坂に通常のSHIORI入口と、Utatane内部用のセッション・SAORI接続を追加しました。
- 蒼空を、バイナリを提供しない手動導入項目としてカタログに追加しました。
- カタログとパッケージの検証、梱包後のABIテスト、Apple Silicon・Intel向けのCI定義を追加しました。
- 利用者向けの導入案内、開発手順、カタログAPIの説明を整備しました。
- Python環境と依存管理をuvに統一しました。
- 配布ZIPを使ってUtatane側の結合テストを実行するコマンドを追加しました。
- 美坂のPLUGIN/2.0電文と、本文を持たないヘッダーのみの応答に対応しました。
- 同じ美坂dylibを共通導入とゴースト・プラグインへの同梱に使用できるようにしました。
- Utatane本体からSHIORIのビルド補助と開発資料を移し、kagariのアプリ同梱処理を集約しました。
