[project]
name = "utatane-modules"
version = "0.1.0"
description = "Build and validate Utatane modules and catalogs"
requires-python = ">=3.12"
dependencies = ["jsonschema==4.26.0"]

[tool.uv]
package = false
required-version = ">=0.6.14"
